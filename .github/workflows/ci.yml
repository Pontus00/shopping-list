name: CI/CD yml Workflow

on:
  workflow_dispatch:
    inputs:
      customInput:
        required: false
        type: string
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-test:
    name: Build & Test (.NET ${{ matrix.dotnet }} on ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        dotnet: [8.0.x, 9.0.x]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ matrix.dotnet }}

      - name: Restore NuGet packages
        run: dotnet restore

      - name: Build
        id: build
        run: dotnet build --no-restore

      - name: Run Unit Tests (only if build succeeded)
        if: steps.build.outcome == 'success'
        run: dotnet test --no-build

      
      - name: Smart Context Intelligence
        if: github.event_name == 'pull_request'
        shell: bash
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_AUTHOR: ${{ github.event.pull_request.user.login }}
          PR_HEAD: ${{ github.event.pull_request.head.ref }}
          PR_BASE: ${{ github.event.pull_request.base.ref }}
          SUMMARY_FILE: $GITHUB_STEP_SUMMARY
        run: |
          echo "=== Smart Context Intelligence ==="
          echo "PR Number: $PR_NUMBER"
          echo "Author: $PR_AUTHOR"
          echo "Head Branch: $PR_HEAD"
          echo "Base Branch: $PR_BASE"
          echo
          # Optionally write to Step Summary for visibility in Actions UI
          {
            echo "## Smart Context Intelligence"
            echo "- **PR Number**: $PR_NUMBER"
            echo "- **Author**: $PR_AUTHOR"
            echo "- **Head Branch**: \`$PR_HEAD\`"
            echo "- **Base Branch**: \`$PR_BASE\`"
          } >> "$SUMMARY_FILE"
        
          
          
          
          






